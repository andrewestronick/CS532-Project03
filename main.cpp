#include <iostream>
#include "edge.h"
#include "graph.h"

using namespace std;

int main()
{
    Edge e;
    e.setStart(1);
    cout << "start=" << e.getStart() << endl;

    // Graph g(5, 0, 0);


    return 0;
}

